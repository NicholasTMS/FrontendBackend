# BanjirAssist Backend

Spring Boot backend with Google ID token verification and JPA persistence.

## Setup

Update `src/main/resources/application.properties` with your Postgres credentials and Google client ID.

## Run

```
./mvnw spring-boot:run
```

